{
  "name": "freshr-server",
  "version": "1.0.0",
  "private": "true",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "dev": "concurrently \"nodemon index\" \"cd client && yarn start\"",
    "build": "cd client && npm install && npm run build"
  },
  "keywords": [],
  "author": "Karam Issa",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  },
  "engines": {
    "node": "v14.16.1"
  }
}
